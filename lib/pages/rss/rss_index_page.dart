import 'package:flutter/cupertino.dart';

class RssIndexPage extends StatelessWidget {
  const RssIndexPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("RssIndexPage"),
    );
  }
}
