import 'package:flutter/material.dart';

class RssReadPage extends StatelessWidget {
  const RssReadPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("RssReadPage"),
    );
  }
}
