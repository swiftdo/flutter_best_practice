import 'package:flutter/material.dart';

class RssCatesPage extends StatelessWidget {
  const RssCatesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
